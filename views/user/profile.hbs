<section>
    <div class="row">
        <div class="col-md-6 m-auto"> 
            <div class="container mt-5">
                <form action="" method="post"></form>
                    <div class="row d-flex justify-content-center">
                        <div class="form-group mb-3 mt-5 text-center">
                            <h1>{{user.name}}</h1>
                        </div>
                        <div class="form-group mb-4 text-center">
                            <h3>User ID : {{user._id}}</h3>
                        </div>
                        <div class="form-group mb-3 text-center">
                            <h3>E-mail : {{user.email}}</h3>
                        </div>
                    </div>

                    <div class="row mb-4 d-flex justify-content-center text-center">
                        <a href="/update-details" style="width: 232px;" class="btn btn-success mt-4">Update details</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>

